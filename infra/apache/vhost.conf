<VirtualHost *:8080>
    ServerName localhost

    ProxyPreserveHost On
    ProxyRequests Off

    # Исключение — не проксировать на Next.js
    ProxyPass /server-status !

    # Next.js (API + SSR)
    ProxyPass / http://app:3000/
    ProxyPassReverse / http://app:3000/

    # Стрим для OpenRouter
    SetEnv proxy-nokeepalive 1
    SetEnv proxy-sendchunked 1
    SetEnv proxy-sendcl 0
    SetEnv force-proxy-request-1.0 0

    RequestHeader set X-Real-IP "%{REMOTE_ADDR}s"
    RequestHeader set X-Forwarded-For "%{X-Forwarded-For}i"
    RequestHeader set X-Forwarded-Proto "http"

    # Таймауты для OpenRouter
    ProxyTimeout 120
    Timeout 120

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>